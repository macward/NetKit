# Task: Unit Tests

## Objective
Create comprehensive unit tests for all NetKit components.

## Context
- Related files:
  - `Tests/NetKitTests/`
- Dependencies: ALL previous tasks
- Branch: `feature/tests`

## Steps
1. [x] Create test directory structure
2. [x] Test `HTTPMethod` - raw values
3. [x] Test `NetworkError` - error cases and equality
4. [x] Test `EmptyResponse` - init and Equatable
5. [x] Test `Environment` - defaults
6. [x] Test `Endpoint` - defaults via test struct
7. [x] Test `RetryPolicy` - retry logic, delays, exponential backoff
8. [x] Test `ResponseCache` - store, retrieve, TTL expiration, invalidation
9. [x] Test `Interceptor` implementations:
   - AuthInterceptor header injection
   - LoggingInterceptor (verify no crash)
10. [x] Test `MockNetworkClient`:
    - Stubbing responses
    - Stubbing errors
    - Call counting
    - Reset
11. [x] Test `URLRequest+Extensions` and `RequestBuilder`:
    - URL building
    - Header merging
    - Timeout handling
    - Fluent API

## Acceptance Criteria
- [x] All tests pass (40 tests)
- [x] Good coverage of happy paths
- [x] Good coverage of error paths
- [x] Tests are isolated (no network calls)
- [x] Tests run fast (~0.2s)

## Notes
- Use URLProtocol subclass to mock URLSession if needed
- Or test NetworkClient through MockNetworkClient for integration-style tests
- Focus on behavior, not implementation details
