# Task: Endpoint Protocol

## Objective
Create the Endpoint protocol with associated Response type, enabling type-safe API endpoint definitions.

## Context
- Related files:
  - `Sources/NetKit/Core/Endpoint.swift`
- Dependencies: `001-models-foundation.task` (needs HTTPMethod)
- Branch: `feature/endpoint-protocol`

## Steps
1. [x] Create `Sources/NetKit/Core/` directory
2. [x] Create `Endpoint.swift` with protocol definition:
   - `associatedtype Response: Decodable`
   - `var path: String { get }`
   - `var method: HTTPMethod { get }`
   - `var headers: [String: String] { get }`
   - `var queryParameters: [String: String] { get }`
   - `var body: Encodable? { get }`
3. [x] Add extension with default implementations for headers, queryParameters, body

## Acceptance Criteria
- [x] Protocol is `public`
- [x] Only `path` and `method` are required (others have defaults)
- [x] Response type is constrained to Decodable
- [x] Default headers and queryParameters return empty dictionaries
- [x] Default body returns nil

## Notes
- This is the core abstraction - keep it minimal
- body is `Encodable?` to support any encodable type
