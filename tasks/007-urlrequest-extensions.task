# Task: URLRequest Extensions

## Objective
Create helper extensions on URLRequest for building requests from endpoints.

## Context
- Related files:
  - `Sources/NetKit/Extensions/URLRequest+Extensions.swift`
- Dependencies:
  - `001-models-foundation.task`
  - `002-endpoint-protocol.task`
- Branch: `feature/urlrequest-extensions`

## Steps
1. [x] Create `Sources/NetKit/Extensions/` directory
2. [x] Create `URLRequest+Extensions.swift` with:
   - `init(endpoint:environment:)` - build URLRequest from Endpoint + Environment
   - Handle path construction with baseURL
   - Handle query parameter encoding
   - Handle body encoding (JSON)
   - Set HTTP method
   - Merge headers (environment defaults + endpoint headers)
3. [x] Add timeout configuration support

## Acceptance Criteria
- [x] Correctly builds full URL from baseURL + path
- [x] Properly encodes query parameters
- [x] Properly encodes JSON body
- [x] Sets Content-Type header for JSON body
- [x] Respects timeout from environment
- [x] Throws appropriate errors for invalid URL construction

## Notes
- Query params: use URLComponents for proper encoding
- Body encoding: use JSONEncoder
- Consider making encoder configurable (date strategies, etc.)
