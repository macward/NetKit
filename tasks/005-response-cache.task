# Task: Response Cache

## Objective
Create a simple in-memory response cache for caching API responses.

## Context
- Related files:
  - `Sources/NetKit/Cache/ResponseCache.swift`
- Dependencies: None
- Branch: `feature/response-cache`

## Steps
1. [x] Create `Sources/NetKit/Cache/` directory
2. [x] Create `ResponseCache.swift` with:
   - In-memory storage (actor for thread safety)
   - `store(data:for:ttl:)` method
   - `retrieve(for:) -> Data?` method
   - `invalidate(for:)` method
   - `invalidateAll()` method
3. [x] Add cache key generation from URLRequest
4. [x] Add TTL (time-to-live) support with automatic expiration
5. [x] Add optional max size limit

## Acceptance Criteria
- [x] Thread-safe (use actor)
- [x] Respects TTL - expired entries return nil
- [x] Can invalidate single entry or all entries
- [x] Cache key based on URL + method + relevant headers
- [x] All types are `public`

## Notes
- Keep it simple - in-memory only, no disk persistence
- Consider NSCache for automatic memory pressure handling
- Cache key should include query parameters
