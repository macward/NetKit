# Task: Response Cache

## Objective
Create a simple in-memory response cache for caching API responses.

## Context
- Related files:
  - `Sources/NetKit/Cache/ResponseCache.swift`
- Dependencies: None
- Branch: `feature/response-cache`

## Steps
1. [ ] Create `Sources/NetKit/Cache/` directory
2. [ ] Create `ResponseCache.swift` with:
   - In-memory storage (actor for thread safety)
   - `store(data:for:ttl:)` method
   - `retrieve(for:) -> Data?` method
   - `invalidate(for:)` method
   - `invalidateAll()` method
3. [ ] Add cache key generation from URLRequest
4. [ ] Add TTL (time-to-live) support with automatic expiration
5. [ ] Add optional max size limit

## Acceptance Criteria
- [ ] Thread-safe (use actor)
- [ ] Respects TTL - expired entries return nil
- [ ] Can invalidate single entry or all entries
- [ ] Cache key based on URL + method + relevant headers
- [ ] All types are `public`

## Notes
- Keep it simple - in-memory only, no disk persistence
- Consider NSCache for automatic memory pressure handling
- Cache key should include query parameters
