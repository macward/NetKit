# Task: Interceptor Protocol & Implementations

## Objective
Create the Interceptor protocol and provide AuthInterceptor and LoggingInterceptor implementations.

## Context
- Related files:
  - `Sources/NetKit/Interceptors/Interceptor.swift`
  - `Sources/NetKit/Interceptors/AuthInterceptor.swift`
  - `Sources/NetKit/Interceptors/LoggingInterceptor.swift`
- Dependencies: None (protocol is standalone)
- Branch: `feature/interceptors`

## Steps
1. [ ] Create `Sources/NetKit/Interceptors/` directory
2. [ ] Create `Interceptor.swift` with protocol:
   - `func intercept(request: URLRequest) async throws -> URLRequest`
   - `func intercept(response: HTTPURLResponse, data: Data) async throws -> Data`
3. [ ] Add default empty implementations via extension
4. [ ] Create `AuthInterceptor.swift`:
   - Token provider closure for header injection
   - Optional token refresh handler
5. [ ] Create `LoggingInterceptor.swift`:
   - Configurable log levels (none, minimal, verbose)
   - Log request method, URL, headers, body
   - Log response status, headers, body

## Acceptance Criteria
- [ ] Protocol methods have default implementations (do nothing)
- [ ] AuthInterceptor injects Authorization header
- [ ] AuthInterceptor supports async token refresh
- [ ] LoggingInterceptor respects configured log level
- [ ] All types are `public`

## Notes
- Interceptors run in order: request interceptors forward, response interceptors reverse
- Use OSLog or print for logging (keep it simple, no external deps)
