# Task: Models Foundation

## Objective
Create the foundational model types that all other components depend on: HTTPMethod, NetworkError, EmptyResponse, and Environment protocol.

## Context
- Related files:
  - `Sources/NetKit/Models/HTTPMethod.swift`
  - `Sources/NetKit/Models/NetworkError.swift`
  - `Sources/NetKit/Models/EmptyResponse.swift`
  - `Sources/NetKit/Models/Environment.swift`
- Dependencies: None (first task)
- Branch: `feature/models-foundation`

## Steps
1. [x] Create `Sources/NetKit/Models/` directory
2. [x] Create `HTTPMethod.swift` - enum with get, post, put, patch, delete cases
3. [x] Create `NetworkError.swift` - enum with all error cases (invalidURL, noConnection, timeout, unauthorized, forbidden, notFound, serverError, decodingError, encodingError, unknown)
4. [x] Create `EmptyResponse.swift` - empty Decodable struct for endpoints without response body
5. [x] Create `Environment.swift` - protocol with baseURL, defaultHeaders, timeout + default extensions

## Acceptance Criteria
- [x] All types are `public`
- [x] HTTPMethod raw values match HTTP standard ("GET", "POST", etc.)
- [x] NetworkError conforms to Error
- [x] EmptyResponse conforms to Decodable and Equatable
- [x] Environment has sensible defaults (empty headers, 30s timeout)

## Notes
- These are leaf dependencies - no imports needed beyond Foundation
- Keep EmptyResponse simple - just needs empty init()
